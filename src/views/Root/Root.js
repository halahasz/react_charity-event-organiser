import React from 'react';
import Button from 'components/Button/Button';

const Root = () => (
  <div>
    <h1>Hello Halina</h1>
    <Button>Save</Button>
    <Button red>Remove</Button>
  </div>
);

export default Root;
